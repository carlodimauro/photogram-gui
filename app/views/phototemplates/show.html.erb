<h1>Photo Details</h1>

<dl>
  <dt>Image</dt>
    <dd><img src="<%= @currentphoto.image %>"></dd>

  <dt>Caption</dt>
    <dd><%= @currentphoto.caption %></dd>

<dt>Owner</dt>
    <dd><%= @currentphoto.poster.username %></dd>

<dt>Posted</dt>
    <dd><%= @currentphoto.created_at %></dd>
    
<dt>Edit photo</dt>
    <dd>Form</dd>

<dt>Delete photo</dt>
    <dd><a href="/delete_photo/<%= @currentphoto.id  %>">Delete Photo</a></dd>

</dl>



<h2>Comments</h2>

<table border="1px">
  <tr>
    <th>
      Commenter
    </th>

    <th>
      Comment
    </th>

    <th>
      Posted
    </th>
  </tr>

<% @currentphoto.comments.each do |a_comment| %>

  <tr>
    <td>
      <%= a_comment.commenter.username%>
    </td>

    <td>
       <%= a_comment.body %>
    </td>

    <td>
      <%= a_comment.created_at %>
    </td>
  </tr>
<% end %>
</table>

</br>
<h3>Add a new comment</h3>
Form
